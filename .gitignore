# Dependencias
node_modules/

# Variables de entorno (contienen credenciales sensibles)
.env

# Logs generados en runtime
logs/

# Archivos subidos por usuarios (avatares, etc.) - se mantiene la carpeta, no su contenido
uploads/*
!uploads/.gitkeep

# Archivos del sistema operativo / editor
.DS_Store
.vscode/