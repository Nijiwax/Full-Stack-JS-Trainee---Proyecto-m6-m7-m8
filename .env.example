# El archivo ".env" NUNCA debe subirse al repositorio (ver .gitignore).
PORT=3000

# Conexión a PostgreSQL
DB_NAME=abp_m7
DB_USER=postgres
DB_PASSWORD=12345
DB_HOST=localhost
DB_PORT=5432

# Módulo 8: JSON Web Tokens
# JWT_SECRET debe ser una cadena larga y aleatoria - nunca la compartas ni la subas al repo real
JWT_SECRET=cambia_esto_por_una_clave_secreta_larga_y_aleatoria
JWT_EXPIRES_IN=1h